package com.google.code.p.Mallory
{
	public class Utilities
	{
		

		
		
		/*
		
		//Rounds a real number to a specified number of decimal places
		public static function RoundTo(...arguments):Number {
			var DefaultPrecision:uint = 8;
			var max:Number = Number.MAX_VALUE;
			var min:Number = Number.MIN_VALUE;
			//Designates large numbers as infinity
			if (arguments[0]>=max) {
				return Number.POSITIVE_INFINITY;
			}
			if (arguments[0]<=-1*max) {
				return Number.NEGATIVE_INFINITY;
			}
			if(Math.abs(arguments[0])<=min){
				return 0;			
			}
	
			if (arguments[1]) {
				return Math.round(arguments[0]*Math.pow(10, arguments[1]))/(Math.pow(10, arguments[1]));
			} else {
				return Math.round(arguments[0]*Math.pow(10, DefaultPrecision))/(Math.pow(10, DefaultPrecision));
			}
		}
		
		public static function radiansToDegrees(r:Number):Number{
			return 180/Math.PI*r;			
		}
		
		public static function degreesToRadians(d:Number):Number{
			return Math.PI/180*d;
		}
	
		public static function CloneArray(newArray:Array):Array{
			var clonedArray:Array = new Array();
			for each(var element:* in newArray){
				clonedArray.push(element);
			}
			return clonedArray;
			
		}
	
	
	
		public static function MapArray(elements:Array, mapper:Object):Array{
			var mappedArray:Array = new Array();
			for(var i:int = 0; i < elements.length; i++){
				mappedArray.push(mapper(elements[i]));
			}
			return mappedArray;
		}	

		public static function isMember(value:*,group:Array):Boolean{
		
			for each (var i:* in group){
				if(value == i) return true;
			}
			
			return false;
		
		}
	
	
	*/
	
	
	
			
		
	}
}